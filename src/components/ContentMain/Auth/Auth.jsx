import React from "react";
import authCSS from "./Auth.module.css";

const Auth = (props) => {
    return (
			<div className={authCSS.listProducts}>
				Авторизация
			</div>
		)
}

export default Auth;